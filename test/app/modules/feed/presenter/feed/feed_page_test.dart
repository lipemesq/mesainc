import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ps_mesainc/app/modules/feed/presenter/feed/feed_page.dart';

void main() {
  testWidgets('FeedPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FeedPage(title: 'Feed')));
    //  final titleFinder = find.text('Feed');
    //  expect(titleFinder, findsOneWidget);
  });
}
